proc main() {
  local int[] m; m = new int[3];
  m[0] = 1 + (sum(m));
  m[2] = 1 + (sum(m));
  output 1 + (sum(m));
}

fun int sum(int[] a) {
  local int n; n = 0;
  local int i; i = 0;
  while (i < (a.length)) do {
    n = n + (a[i]);
    i = i + 1;
  }
  return n;
}
